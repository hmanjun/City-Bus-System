<div class="d-flex flex-column align-items-center">
    <h1 class="text-light">{{location.name}}</h1>
    <div class="d-flex flex-column align-items-center">
        <h1>Add</h1>
        <div class="d-flex flex-row justify-content-center">
            <button type="button" class="btn btn-light" id="add-route-btn">Add Route</button>
            <button type="button" class="btn btn-light" id="add-stop-btn">Add Stop</button>
        </div>
    </div>
</div>